# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/voice-ivr.p12
# The password used to generate the certificate
server.ssl.key-store-password=mebxbylx1
# The alias mapped to the certificate
server.ssl.key-alias=voice-ivr
#FTP Host
ftp.server.host=192.168.1.86
#FTP username
ftp.server.username=podkmax
#FTP password
ftp.server.password=mebxbylx1
debug=true
# JSON Path
json.file.path=/src/main/resources/

#=======================
#google; vn
mrcp.connection = google

#===============================================
#db
spring.h2.console.enabled=true

spring.datasource.url=jdbc:h2:file:~/test
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
